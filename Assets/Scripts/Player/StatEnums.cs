using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatEnums : MonoBehaviour {

	enum Stats {
		Str = 1,
		Dex = 2,
		Con = 3,
		Int = 4,
		Wis = 5,
		Cha = 6
	}

	// Use this for initialization
	void Start () {
		//Stats = 
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
